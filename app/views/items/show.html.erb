<div class="container">
  <div class="row"></div>
  <div class="col-sm-3">
    <h5><%= @item.name %></h5>
    <%= image_tag(@item.image_url) %>
    <p>
      <%= @item.description %>
    </p>
    <p>
    <%= button_to "Purchase", orders_path(item_id: @item.id, user_id: User.first.id) %>
    </p>
  </div>
</div>
